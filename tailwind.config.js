// tailwind.config.js
export default {
  darkMode: "class", // use class-based toggling
  theme: {
    extend: {
      colors: {
        background: {
          DEFAULT: "var(--background)",
        },
      },
    },
  },
}
